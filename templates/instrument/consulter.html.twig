{% extends 'base.html.twig' %}

{% block title %}Instrument Lister !{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

        .tab-lister-instrument{
            width: 100%;
            height: 100vh;
            font-family: "Nunito", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
        }

        .tab-lister-instrument {
            border-collapse: collapse;
            margin: 20px 0;
            border-spacing: 0;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #d9d9d9;
        }

    </style>

    <div class="tab-lister-instrument">

        <H1>Information sur {{ instrument.type.libelle }}</H1>
        <p>Numéro de Série : {{ instrument.numSerie }}</p>
        <p>Date d'achat {{ instrument.dateAchat|date('d/m/Y') }}</p>
        <p>Utilisation : {{ instrument.utilisation }}</p>
        <p> - En classe : Seulement en classe <br> - Prêt : Possibilité de prêt pour un élève</p>
        <p>Toutes les interventions :</p>

        <table>
            <thead>
            <tr>
                <th>Date Debut</th>
                <th>Date Fin</th>
                <th>Prix</th>
                <th>Contrat de prêt</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
            </thead>
            {% for i in pIntervention %}
                <tr>
                <td>{{ i.dateDebut|date('d/m/Y')}}</td>
                <td>{{ i.dateFin|date('d/m/Y')}}</td>
                <td>{{ i.prix}}</td>
                <td><a href="#">Voir le contrat</a></td>
                <td>{{ i.descriptif}}</td>
                <td>
                    <a href="#">✏️</a>
                    <a href="#">👁️</a>
                    <a href="#">🗑️</a>
                </td>
            {% else %}
                <tr>
                    <th>Aucune intervention n'a été trouvée.</th>
                </tr>
            {% endfor %}
        </table>

    </div>
{% endblock %}
