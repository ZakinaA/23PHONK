<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

LISTE DES COURS</br>
<table>
    <tr>
        <th>Jour</th>
        <th>Heure de début</th>
        <th>Heure de fin</th>
        <th>Instrument</th>
        <th>Âge minimum</th>
        <th>Nombre de places max</th>
    </tr>
    {% for c in pCours %}
        <tr>
        <td>{{ c.jour.libelle }}</a></td>
        <td>{{ c.heureDebut|date('H:i:s')}}</a></td>
        <td>{{ c.heureFin|date('H:i:s')}}</a></td>
        <td>{{ c.typeInstrument.libelle }}</td>
        <td>{{ c.ageMini}}</td>
        <td>{{ c.nbPlaces}}</td>
    {% else %}
        <tr>
            <td>Aucun cours n'a été trouvé.</td>
        </tr>
    {% endfor %}
</table>


</body>
</html>